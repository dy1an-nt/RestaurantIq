---
name: migrate
description: "Author and apply a RestaurantIQ database migration through the tracked runner. Use whenever a schema change is needed (new table, column, constraint, or index) or when migrations must be applied to local, staging, or production: \"add a migration\", \"run migrations\", \"apply 026 to prod\". Never paste SQL into the Supabase SQL editor for production changes."
---

# Migrate

The canonical procedure is in
[`../../../.claude/skills/migrate/SKILL.md`](../../../.claude/skills/migrate/SKILL.md).

Before acting, read that file completely and follow it. Resolve any relative
references from the canonical skill directory. This wrapper exists only so
Codex can discover the shared skill; do not duplicate the procedure here.
