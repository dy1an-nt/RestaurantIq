---
name: tdd
description: Use test-driven development for a bug with a cheap, focused regression target. Use when the user requests TDD, a failing test, or a regression test, or when a backend bug has an obvious local Jest target.
---

# Test-driven bug fix

The canonical procedure is in
[`../../../.claude/skills/tdd/SKILL.md`](../../../.claude/skills/tdd/SKILL.md).

Before acting, read that file completely and follow it. Resolve any relative
references from the canonical skill directory. This wrapper exists only so
Codex can discover the shared skill; do not duplicate the procedure here.
